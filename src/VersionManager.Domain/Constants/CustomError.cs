

namespace VersionManager.Domain.Constants
{
    public sealed class CustomError
    {
        public const string BASE_CLASS = "This method must be implemented in the derived class.";
        public const string RELEASE_VERSION_SEND_TO_RELEASE = "The version is already in release.";
    }
}
